import 'package:flutter/material.dart';

class CityShowUi extends StatelessWidget {
  const CityShowUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('City Show'),
      ),
      body: Container(),
    );
  }
}
